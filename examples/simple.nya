fn print_int(_: i64);
fn read_int() -> i64;

fn max(a: i32, b: i32) -> i32 {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

fn main() {
    let a: i64 = read_int();
    let b = read_int();
    print_int(max(a, b));
}