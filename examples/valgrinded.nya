fn print_int(_: i64);

fn fib(n: i64) -> i64 {
    if (n == 0) {
        return 1;
    }

    if (n == 1) {
        return 1;
    }

    return fib(n-1) + fib(n-2);
}

fn main() {
    print_int(fib(6));
}