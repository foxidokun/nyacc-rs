stages:
  - build
  - test
  - release

build:
  stage: build
  image: rust_custom
  script:
    - cargo build
  cache:
    paths:
      - target

runtest:
  stage: test
  image: rust_custom
  cache:
    paths:
      - target
  script:
    - cargo test

codestyle:
  stage: test
  image: rust_custom
  cache:
    paths:
      - target
  script:
    - cargo fmt --check

build_release:
  stage: release
  image: rust_custom
  only:
  - master
  cache:
    paths:
      - target
  script:
    - cargo build --release
  artifacts:
    paths:
      - target/release/nyacc